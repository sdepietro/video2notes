# Copiá este archivo a .env y completá tu key:  cp .env.example .env
OPENAI_API_KEY=sk-...

# Opcionales (tienen valores por defecto en config.py)
# Modelo de transcripción (STT)
STT_MODEL=gpt-4o-transcribe
# Modelo para generar la minuta
LLM_MODEL=gpt-4o
